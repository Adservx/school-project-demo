import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// AI-optimized: Minimal imports for fast load times
// No reportWebVitals or external CSS libraries

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
